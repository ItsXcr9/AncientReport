[agent]
hostname = "auto-detect"
collection_interval = "10s"

[clickhouse]
url = "http://localhost:6123"
database = "AncientReport"
username = "AncientReport"
password = "AncientReport"

[nats]
# NATS JetStream URL for streaming mode (V2)
url = "nats://localhost:4222"
enabled = true

[ai]
provider = "google"  # Options: anthropic, openai, google
api_key = ""  # Set your API key here or use env var GEMINI_API_KEY
model = "gemini-2.5-flash-lite"

[alerts]
# telegram_token = "your-bot-token"
# telegram_chat_id = "your-chat-id"
# slack_webhook = "https://hooks.slack.com/services/..."
